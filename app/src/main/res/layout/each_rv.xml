<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="20dp"
    android:backgroundTint="#022E50"
    android:elevation="10dp"
    android:padding="20dp"
    android:background="@drawable/border_todo"

    app:layout_marginBaseline="20dp">

    <TextView
        android:id="@+id/titletodo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:layout_marginStart="16dp"
        android:text="Title"
        android:textColor="#EDF7F6"
        android:textSize="24sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView
        android:id="@+id/descrption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_marginTop="12dp"
        android:text="Description"
        android:textColor="#EDF7F6"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="@+id/titletodo"
        app:layout_constraintTop_toBottomOf="@+id/titletodo" />

    <TextView
        android:id="@+id/date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Date"
        android:textColor="#EDF7F6"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="@+id/descrption"
        app:layout_constraintTop_toBottomOf="@+id/descrption" />

</androidx.constraintlayout.widget.ConstraintLayout>